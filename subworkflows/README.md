# Subworfkflow

This is the directory to store composed sub-workflows, where each is of format *.nf* and is already a workflow that considers of smaller module processes or scripts in [modules dir](../modules/README.md)