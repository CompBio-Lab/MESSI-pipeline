/*
  Parameters used in the splitting stage

  Seed is set to reproduce indices for same splits
  # TODO: Need to dynamically update it, when run this entire pipeline various times
          to mimic repeated K fold cross validation
*/

params {
  dummy_splitting = "placeholder"
}