# Modules

This is the directory to store all types of scripts used mainly of `*.R`, `*.py`, `*.nf` s, where each non `*.nf` sources codes scripts are handled within the `*.nf` files (the processes), and later wrapped in higher level sub-workflows in the [subworkflow dir](../subworkflow/README.md)